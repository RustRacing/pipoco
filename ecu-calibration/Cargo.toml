[package]
name = "ecu-calibration"
version = "0.1.0"
edition = "2021"

[lib]
name = "ecu_calibration"
path = "src/lib.rs"

[dependencies]
ecu-domain = { path = "../ecu-domain" }

[features]
default = []
