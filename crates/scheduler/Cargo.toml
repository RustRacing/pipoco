[package]
name = "ecu-scheduler"
version = "0.1.0"
edition = "2021"

[lib]
name = "ecu_scheduler"
path = "src/lib.rs"

[dependencies]
ecu-board-api = { path = "../board-api" }
ecu-domain = { path = "../domain" }
ecu-io = { path = "../io" }

[dev-dependencies]
ecu-calibration = { path = "../calibration" }
ecu-runtime = { path = "../runtime" }
ecu-spec = { path = "../spec" }
ecu-test-fixtures = { path = "../test-fixtures" }

[features]
default = []

[lints]
workspace = true
