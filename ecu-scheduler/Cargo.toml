[package]
name = "ecu-scheduler"
version = "0.1.0"
edition = "2021"

[lib]
name = "ecu_scheduler"
path = "src/lib.rs"

[dependencies]
ecu-domain = { path = "../ecu-domain" }

[dev-dependencies]
ecu-runtime = { path = "../ecu-runtime" }
ecu-spec = { path = "../ecu-spec" }

[features]
default = []
