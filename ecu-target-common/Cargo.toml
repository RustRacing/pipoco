[package]
name = "ecu-target-common"
version = "0.1.0"
edition = "2021"

[lib]
name = "ecu_target_common"
path = "src/lib.rs"

[dependencies]
ecu-core = { path = "..", default-features = false }
usb-device = "0.2"
usbd-serial = "0.1"
embedded-hal = "1.0"

[features]
default = []
